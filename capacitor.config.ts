import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'u21566641_HW02',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
